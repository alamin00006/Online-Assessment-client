"use client";
// Candidate login page wrapper for the assessment client.
import { AuthLoginPage } from "@/components/auth";
// Renders the candidate authentication entry point.
const CandidateLogin = () => {
  return <AuthLoginPage role="candidate" />;
};

export default CandidateLogin;



